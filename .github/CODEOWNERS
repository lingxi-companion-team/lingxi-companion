# 灵犀学伴 —— 代码归属（CODEOWNERS）
#
# 作用：配合 ruleset 的 require_code_owner_review 生效 ——
#   命中某条规则的 PR，必须得到**该行所列 owner 之一**的 approve 才能合并。
#   没有本文件时该规则是**空转**的（GitHub 找不到 owner，只能放行）。
#
# 读取位置：GitHub 读取 **PR 目标分支**上的本文件。
#   日常 PR 目标是 develop，故本文件必须存在于 develop；
#   同时保留在 main 上，供 release/* → main 的发布 PR 使用。
#
# 语法要点
#   - 一个路径**后出现的规则覆盖先出现的**（不是「最精确优先」）
#     → 先写兜底 `*`、再写具体目录，顺序才是对的。
#   - 路径以 / 开头 = 相对仓库根；结尾带 / 表示该目录下全部内容。
#   - 同一行可写多个 owner：**任一**批准即可满足要求（不是全部）。
#   - owner 必须是**已有本仓库写权限**的账号（@用户名）或 Team（@org/team）。
#     本组织**没有创建任何 Team**，只能用个人账号。
#   - 含无效 owner 的行会被 GitHub 整行跳过。校验方式：
#     gh api repos/lingxi-companion-team/lingxi-companion/codeowners/errors
#
# ⚠️ 两条已知局限（别指望它做它做不到的事）
#
#   1. **作者不能批准自己的 PR**（GitHub 硬规则）。因此若某路径**只有作者
#      一个 owner**，该 PR 的 code owner 要求将**永远无法满足**（死锁）。
#      → 所以下面每条路径都至少留 **2 个 owner**，保证总有「非作者」可批。
#      → 将来若某目录只留 1 个 owner，务必确认该作者不会改自己这块。
#
#   2. CODEOWNERS 只能要求「所列 owner 中**至少一人**批准」，
#      **无法**表达「冻结文件必须三人全部批准」。后者只能靠流程纪律
#      （见 不推送/docs/feature-branch-workflow.md §6）。
#
# 当前分工（2026-09-15 确认）
#   A → @lxy770        行为智能体 + 融合层 + 标注规范
#   C → @rainwind0408  环境智能体 + 前端 + 集成发布
#   B → **尚未加入协作者**（账号未知）→ 其目录暂走兜底
#
# ⚠️ 要说清的现实：@rainwind0408 是仓库管理员，两个 ruleset 都给了
#   Admin bypass，所以 **CODEOWNERS 技术上拦不住 C 自己**。
#   这道门禁真正约束的是 @lxy770（write 权限，无 bypass），
#   以及将来以 write 身份加入的 B。
#
# ---------------------------------------------------------------------------

# ── 兜底：未被下面规则单独覆盖的路径 ───────────────────────────────────
# 现阶段只有两个可审阅账号，故先兜底到「任一可写成员」，
# 保证 require_code_owner_review 立刻具备实际约束力（而不是空转）。
*                             @rainwind0408 @lxy770

# ── 成员 A（@lxy770）────────────────────────────────────────────────────
/agents/behavior/             @lxy770 @rainwind0408
/fusion/                      @lxy770 @rainwind0408
/tests/agents/                @lxy770 @rainwind0408
/tests/fusion/                @lxy770 @rainwind0408
/docs/annotation_spec/        @lxy770 @rainwind0408

# ── 成员 C（@rainwind0408）──────────────────────────────────────────────
/agents/env/                  @rainwind0408 @lxy770
/app/                         @rainwind0408 @lxy770
/common/mock/                 @rainwind0408 @lxy770
/tests/stress/                @rainwind0408 @lxy770
/docs/reports/                @rainwind0408 @lxy770

# ── 成员 B：尚未加入协作者 ──────────────────────────────────────────────
# 非协作者的 owner 会被忽略（等于没写），所以先留空、走上面的兜底 `*`。
# B 加入后取消注释、填入账号即可（每行仍保留第二个 owner，避免自批死锁）：
# /agents/expression/         @<B 的账号> @lxy770
# /pipeline/                  @<B 的账号> @lxy770
# /tests/pipeline/            @<B 的账号> @lxy770

# ── 共享冻结层：改一行需三方评审（见 workflow §6）──────────────────────
# 这两行列出的是「三人」，但 CODEOWNERS 只能要求其中一人批准，
# 实际作用只是「任何改动至少惊动一名 owner」，无法自动强制三人全批。
/common/perception_types.py   @lxy770 @rainwind0408
/common/agent_base.py         @lxy770 @rainwind0408
/common/config.py             @lxy770 @rainwind0408

# ── 门禁自身：由管理员单独持有 ─────────────────────────────────────────
# 否则成员可以自行修改 CODEOWNERS / CI / 门禁阈值来放宽约束（自授权）。
# GitHub 官方建议原文：
#   "To protect a repository fully against unauthorized changes, you also
#    need to define an owner for the CODEOWNERS file itself."
#
# ⚠️ 代价：管理员自己改这些文件时，因为「不能自批」会看到 Merging is blocked，
#   需用 Admin bypass 合并。这是**刻意的摩擦** —— 碰门禁就该有明确信号。
/.github/                     @rainwind0408
/configs/                     @rainwind0408
/pyproject.toml               @rainwind0408
/requirements-dev.txt         @rainwind0408
/CONTRIBUTING.md              @rainwind0408
